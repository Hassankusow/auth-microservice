PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=auth_db
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1h

# Password Reset Token expiry (ms) — 15 minutes
RESET_TOKEN_EXPIRES=900000
